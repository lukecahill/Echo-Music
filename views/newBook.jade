extends layout

block content
    h3= title

    form#add-book-form(method="post", action="/addBook")
        div.form-group
            label(for="author_name") Book Title
            input#book-name.form-control(type="text", name="book_name", placeholder="What If?")

        button#add-author.btn.btn-primary(type="submit") 
            span.glyphicon.glyphicon-plus 
            |   Add